---
"@toon-protocol/core": patch
---

Add missing `engines.node` field to packages/core/package.json to match packages/sdk, and remove the stale `packages/rig` entry from tsconfig.json's exclude array.
