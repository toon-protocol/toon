---
---

Fix packages/core/src/nip34/README.md's "Integrate with BLS" section, which documented a nonexistent `@toon-protocol/bls` package, to show `NIP34Handler` wired into the current `createNode()` / `HandlerRegistry` pattern from `@toon-protocol/sdk`.
